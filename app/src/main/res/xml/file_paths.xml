<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches File(context.cacheDir, "updates") where UpdateManager writes the APK. -->
    <cache-path
        name="updates"
        path="updates/" />
</paths>
